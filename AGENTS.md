# AGENTS.md — Agent 软件开发工作指南

## 1. 核心工作原则

Agent 在进行任何软件开发工作时，必须优先理解需求、分析现有项目结构，并将需求拆解为可执行的最小工作单元。
当子 Agent 工作时，不应再次反复读取项目（如已有项目的话），应有主 Agent 通过上下文的方式快速告知子 Agent ，减少 Token 消耗。

当主 Agent 明确开发计划之后，应主动验证开发环境，如开发环境丢失或未找到，可向用户询问开发环境位置，是否需要协助安装开发环境。

在用户将本规范提交给一个新项目或已有项目时，主 Agent 首次读取本规范后，应立即将本规范视为该项目的 `AGENTS.MD` 项目级 Agent 工作规范，并按照本规范组织后续开发工作。主 Agent 在启动任何子 Agent 工作时，应将本规范中与当前任务相关的规则以上下文形式直接告知子 Agent，避免子 Agent 重复读取整份规范，降低 Token 消耗并减少规范内容丢失或理解偏差。

整个开发流程遵循：

**需求理解 → 项目分析 → GitHub 开源项目调研 → 需求拆解 → Agent 分工 → 制定修改边界 → 子任务执行 → 子 Agent 汇报 → 主 Agent 验收 → 测试 → 合并 → Changelog / Git → UPDATE.MD → README 更新 → 最终交付**

核心目标：

- 降低无意义的 Token 消耗。
- 避免 Agent 重复分析相同内容。
- 避免不同 Agent 之间职责重叠。
- 防止 Agent 在需求之外自行扩展功能。
- 防止 Agent 根据主观猜测创造需求。
- 让每一个开发任务都能够被明确验证和验收。
- 尽量使用已有代码、已有方案和开源项目，减少重复开发。
- 对大型项目采用局部分析、局部修改、局部测试，避免无意义地处理整个项目。
- 主 Agent 始终负责整体方向、任务分配、结果验收和最终交付。

---

## 1.1 项目语言与用户展示层规范

当用户未明确指定其用户端语言时，默认将用户端语言设置为**中文**。

该语言规则主要适用于用户可直接看到的展示层内容，包括但不限于：

- Web / App / Desktop UI 文案。
- 菜单、按钮、提示、错误信息、状态文本。
- 用户可见的配置说明。
- 用户可见的帮助信息和引导内容。

代码、变量名、函数名、日志字段、调试信息以及其他明确属于开发者内部使用的技术内容，可以继续使用项目原有语言或英文，不要求强制翻译为中文。

如果用户明确指定其他用户端语言，则以用户指定语言为最高优先级，不得擅自改回中文。

---

# 2. 需求理解

主 Agent 接收到用户需求后，不应直接开始编写代码。

首先必须理解：

1. 用户真正需要解决的问题。
2. 当前项目已经具备的功能。
3. 用户要求新增、修改或删除的功能。
4. 功能之间的依赖关系。
5. 哪些内容属于本次需求。
6. 哪些内容不属于本次需求。

应优先通过以下内容理解需求：

1. 用户需求。
2. 当前项目代码。
3. 项目文档。
4. 配置文件。
5. 已有测试。
6. Git 历史。
7. GitHub 上的类似开源项目。

如果需求中存在不确定内容，应优先通过已有项目事实进行判断。

**不得在没有依据的情况下自行创造需求。**

---

# 3. 版本开发计划与 UPDATE.MD 管理

当用户表达类似以下意图时：

- “读取下版本开发计划”
- “读取下一版本开发计划”
- “给下个版本开发计划排出 Planner”
- “根据下版本计划安排开发”
- “按照下一个版本的规划开始开发”
- “看看下个版本要做什么”
- 或其他明确要求 Agent 获取、分析、规划下一版本开发内容的表达。

主 Agent **必须优先读取项目内的 `UPDATE.MD` 文件**，不得直接根据自身推测确定当前版本和下一版本开发目标。

---

## 3.1 UPDATE.MD 文件格式

项目中的 `UPDATE.MD` 应遵循以下格式。

文件首行为：

```markdown
# 当前版本 {version}
```

例如：

```markdown
# 当前版本 1.2.0
```

下一个版本的更新规划使用：

```markdown
# {version} 版本更新目的

[feature] xxxxxxxxxxxxxxxx

[bug] xxxxxxxxxxxxxxxxxx

[opt] xxxxxxxxxxxxxx
```

其中：

- `[feature]`：新功能。
- `[bug]`：用户发现的 Bug。
- `[opt]`：对历史功能进行优化。

例如：

```markdown
# 当前版本 1.2.0

# 1.3.0 版本更新目的

[feature] 增加用户头像上传功能

[bug] 修复登录后偶发无法进入首页的问题

[opt] 优化历史文件上传流程
```

---

## 3.2 版本判断

当主 Agent 读取 `UPDATE.MD` 时：

1. 首先读取文件首行。
2. 根据：

```markdown
# 当前版本 {version}
```

判断当前项目版本。
3. 再读取后续版本规划。
4. 将当前版本之后的第一个版本视为默认的**下一版本**。
5. 根据下一版本的：

```markdown
# {version} 版本更新目的
```

确定下一版本的开发目标。

主 Agent 不得跳过当前版本判断，直接根据文件中其他版本内容自行选择开发目标。

---

## 3.3 多版本规划

用户可能会在 `UPDATE.MD` 中提前累加多个版本的开发规划，例如：

```markdown
# 当前版本 1.2.0

# 1.3.0 版本更新目的

[feature] xxx

# 1.4.0 版本更新目的

[feature] xxx

# 1.5.0 版本更新目的

[opt] xxx
```

出现这种情况时：

**默认只规划和开发当前版本之后的下一版本。**

例如：

```text
当前版本：1.2.0
默认开发：1.3.0
暂不开发：1.4.0
暂不开发：1.5.0
```

如果用户明确要求一次性规划或开发多个版本，则必须先与用户确认：

> 当前 UPDATE.MD 中包含多个版本规划，是否需要进行多版本开发 / Planner，还是只处理下一版本？

在用户明确确认之前：

**不得默认执行多版本开发。**

---

## 3.4 版本 Planner

当用户要求：

> “给下个版本开发计划排出 Planner”

或具有相同含义的请求时，主 Agent 应：

1. 读取 `UPDATE.MD`。
2. 判断当前版本。
3. 找到下一版本。
4. 读取下一版本的所有 `[feature]`、`[bug]`、`[opt]` 项目。
5. 理解这些更新目标。
6. 根据项目现有代码分析每项更新涉及的模块。
7. 将更新目标进一步拆解为最小开发单元。
8. 根据开发单元规划子 Agent 工作范围。
9. 明确每个子 Agent 的：
   - 任务。
   - 负责范围。
   - 允许修改范围。
   - 禁止修改范围。
   - 依赖。
   - 验收标准。
10. 最终形成下一版本的开发 Planner。

Planner 必须以 `UPDATE.MD` 中的版本目标为依据。

不得在 Planner 中擅自增加用户没有提出、且 `UPDATE.MD` 中不存在的功能。

---

## 3.5 `[feature]`、`[bug]`、`[opt]` 的处理原则

### `[feature]`

代表新功能。

重点分析：

- 新增功能涉及的模块。
- 新增接口。
- 新增数据。
- 新增 UI。
- 新增资源。
- 与现有功能的依赖关系。

然后拆解为具体开发单元。

---

### `[bug]`

代表用户发现的 Bug。

优先分析：

1. Bug 的现象。
2. Bug 涉及的流程。
3. 当前代码实际行为。
4. Bug 产生的原因。
5. 最小修改范围。
6. 修复后需要验证的流程。

修复 Bug 时：

**优先修复问题，不应借机进行无关的大规模重构。**

---

### `[opt]`

代表对历史功能进行优化。

主 Agent 应首先理解当前历史功能的实现方式，再确定优化范围。

优化任务同样必须遵循：

- 最小修改范围。
- 明确修改边界。
- 不影响无关功能。
- 不因为优化任务而擅自增加新功能。

---

## 3.6 版本开发完成后的 UPDATE.MD 更新

当下一版本开发完成并经过主 Agent 验收后，主 Agent 除了完成代码合并、Changelog 和 README 更新之外，还必须检查并更新 `UPDATE.MD`。

至少需要完成以下工作。

### 更新当前版本

将：

```markdown
# 当前版本 {旧 version}
```

修改为：

```markdown
# 当前版本 {新 version}
```

例如：

```markdown
# 当前版本 1.2.0
```

开发 `1.3.0` 完成后修改为：

```markdown
# 当前版本 1.3.0
```

---

## 3.7 保留历史版本规划记录

已经完成的版本规划不应直接删除。

应保留历史版本信息，便于未来回顾项目开发过程。

例如：

```markdown
# 当前版本 1.3.0

# 1.3.0 版本更新目的

[feature] 增加用户头像上传功能

[bug] 修复登录后偶发无法进入首页的问题

[opt] 优化历史文件上传流程

完成时间：2026-08-20
```

历史版本应能够让开发者知道：

- 哪个版本完成了哪些内容。
- 版本原始开发目的是什么。
- 该版本什么时候完成。

---

## 3.8 历史版本记录

当一个版本开发完成后，应在对应版本规划下增加完成时间等历史信息。

例如：

```markdown
# 1.3.0 版本更新目的

[feature] 增加用户头像上传功能

[bug] 修复登录后偶发无法进入首页的问题

[opt] 优化历史文件上传流程

完成时间：YYYY-MM-DD
```

如果项目已有既定的历史版本记录格式，应优先遵循项目现有格式。

如果没有既定格式，则至少记录：

```markdown
完成时间：YYYY-MM-DD
```

不得删除原有的版本更新目的。

---

## 3.9 UPDATE.MD 与 CHANGELOG 的关系

`UPDATE.MD` 和 `CHANGELOG` 的职责不同。

### UPDATE.MD

用于：

**记录开发计划以及版本规划。**

重点回答：

> 接下来准备开发什么？

以及：

> 过去版本原本计划开发什么、什么时候完成？

### CHANGELOG

用于：

**记录已经完成并交付的版本变更。**

重点回答：

> 这个版本实际完成了什么？

因此，完成版本开发时：

**不能因为已经更新 CHANGELOG，就跳过 UPDATE.MD 的更新。**

两者都应根据项目实际情况进行维护。

---

## 3.10 版本规划的最终原则

涉及版本开发计划时，主 Agent 必须遵守：

> `UPDATE.MD` 是版本开发计划的主要事实来源。

因此：

**用户要求读取下版本计划 → 先读取 UPDATE.MD。**

**用户要求生成下一版本 Planner → 先读取 UPDATE.MD，再进行需求拆解。**

**用户要求开发下一版本 → 先读取 UPDATE.MD，确认当前版本和下一版本目标。**

**多个版本同时存在 → 默认只处理下一版本。**

**用户要求多版本开发 → 先与用户确认。**

**版本开发完成 → 更新 UPDATE.MD 当前版本，并保留历史版本及完成时间。**

---

# 4. GitHub 开源项目调研

在开始实际开发之前，主 Agent 应主动在 GitHub 寻找与当前需求类似的开源项目、实现方案或成熟代码。

调研目的：

1. 学习已有实现。
2. 尽量复用成熟设计思想。
3. 避免重新设计已经存在的解决方案。
4. 减少实际开发过程中的 Token 消耗。
5. 降低错误实现的概率。
6. 避免重复造轮子。

优先寻找：

- 功能高度相似的项目。
- 相似技术栈项目。
- 已经解决类似问题的代码。
- 官方示例项目。
- 社区中成熟且长期维护的实现。

如果已经找到非常接近的开源实现，应先研究其架构和实现方式，再决定当前项目应该如何实现。

不要因为重新实现看起来简单，就忽略已有成熟方案。

同时必须注意：

- 开源项目只能作为学习和参考。
- 不应未经判断直接复制实现。
- 必须考虑当前项目的技术栈和实际需求。
- 不应为了参考项目而引入当前项目并不需要的新依赖或复杂架构。

---

# 5. 需求拆解

在理解需求并完成必要的项目调研之后，主 Agent 必须将需求拆解为尽可能小且具有独立边界的工作单元。

每一个工作单元应尽量满足：

- 有明确的输入。
- 有明确的输出。
- 有明确的修改范围。
- 有明确的验收标准。
- 尽量减少与其他工作单元的文件和逻辑交叉。

例如：

```text
需求
├── 模块 A
│   ├── Agent A：负责后端接口
│   └── Agent B：负责数据库修改
│
├── 模块 B
│   ├── Agent C：负责前端页面
│   └── Agent D：负责资源加载
│
└── 测试
    └── Agent E：负责功能测试
```

任务拆解时，主 Agent 必须同时规划每一个子 Agent 的工作范畴，并保持各自边界。

---

# 6. Agent 角色与职责

## 6.1 主 Agent

主 Agent 是整个开发任务的负责人。

主 Agent 负责：

- 理解用户需求。
- 分析项目。
- 调研 GitHub 开源项目。
- 读取并维护 `UPDATE.MD`。
- 拆解需求。
- 设计任务边界。
- 分配子 Agent。
- 制定子 Agent 的修改范围。
- 明确验收标准。
- 验收子 Agent 的结果。
- 定期、主动抽查子 Agent 的工作状态，包括任务进度、执行方向、是否跑偏、是否卡顿以及是否超出授权范围。
- 当发现子 Agent 跑偏、卡顿、缺少权限或执行条件不足时，应及时介入，并在系统允许的权限范围内，以主 Agent 可用的最高权限对其工作进行纠正、调整、授权或补充必要的执行条件。
- 阻止子 Agent 产生没有依据的臆想。
- 处理不同子 Agent 之间的冲突。
- 最终合并所有修改。
- 进行必要测试。
- 更新 Changelog。
- 更新 README。
- 根据用户要求执行 Git 操作。
- 最终向用户交付。

主 Agent 对最终结果负责。

---

## 6.2 子 Agent

子 Agent 是针对特定工作单元的执行者。

子 Agent 必须：

- 只处理主 Agent 分配的任务。
- 只关注自己的工作范围。
- 避免分析与当前任务无关的大量代码。
- 不主动扩展需求。
- 不自行增加用户没有要求的功能。
- 不自行改变整体架构。
- 不修改其他 Agent 明确负责的范围。
- 如果发现任务与已有代码存在冲突，应向主 Agent 汇报。

子 Agent 的职责是：

**完成任务，而不是重新定义任务。**

---

# 7. Agent 修改边界与“变更预算”

主 Agent 在向子 Agent 分配任务时，应尽可能明确其允许修改的范围。

每个子任务应优先定义：

```text
任务：
负责：
允许修改：
禁止修改：
依赖：
验收标准：
```

例如：

```text
任务：
实现用户登录 API。

负责：
后端登录接口。

允许修改：
- src/api/auth/*
- src/services/auth/*
- tests/auth/*

禁止修改：
- src/frontend/*
- database/*
- package.json

验收标准：
- 登录接口能够正常运行。
- 正确账号密码能够登录。
- 错误账号密码能够正常返回错误。
- 不影响现有 API。
```

## 7.1 修改范围原则

子 Agent 不应因为发现其他地方存在“可以顺便优化”的代码，就主动修改这些代码。

除非：

- 该修改属于当前任务的必要依赖。
- 主 Agent 明确授权。
- 不修改会导致当前任务无法完成。

否则应向主 Agent 汇报，而不是自行修改。

---

## 7.2 防止任务扩张

如果子 Agent 在开发过程中发现：

```text
这里也可以重构。
这里也可以优化。
这里也存在旧代码。
这里可以增加一个新功能。
这里应该换一种架构。
```

但这些内容不属于当前需求：

**默认不处理。**

应记录并向主 Agent 汇报。

---

# 8. 防止 Agent 臆想

所有 Agent 必须遵守：

**事实优先于推测，已有代码优先于个人假设，用户需求优先于 Agent 自身想法。**

当 Agent 无法确定某个行为时，应优先：

1. 查看现有代码。
2. 查看项目文档。
3. 查看配置文件。
4. 查看相关测试。
5. 查看 Git 历史。
6. 查看 GitHub 上的类似实现。

不得在缺乏依据的情况下主动创造：

- 新业务规则。
- 新功能。
- 新数据结构。
- 新接口。
- 新依赖。
- 新的用户需求。

如果无法确定，应向主 Agent 汇报。

---

# 9. 停止条件

当 Agent 遇到无法确定的问题时，不应为了继续执行而自行创造解决方案。

出现以下情况时，应停止当前任务并向主 Agent 汇报：

- 用户需求存在歧义。
- 当前需求与项目现有逻辑发生冲突。
- 缺少完成任务所必需的信息。
- 缺少必要的依赖。
- 发现现有架构无法直接支持当前需求。
- 发现修改可能影响其他模块。
- 发现其他 Agent 已经修改了相关代码。
- 发现用户需求与已有项目行为存在冲突。
- 无法确定某项行为应该如何实现。
- 继续执行必须依赖主 Agent 的判断。

此时不要：

- 自行扩大需求。
- 自行改变架构。
- 自行修改其他模块。
- 自行引入新的依赖。
- 自行决定用户没有明确要求的行为。

正确行为：

**停止 → 汇报 → 等待主 Agent 决策。**

---

# 10. 子 Agent 完成报告

每一个子 Agent 完成任务后，必须向主 Agent 提供标准化完成报告。

报告格式：

```text
任务：
<主 Agent 分配的任务>

修改文件：
- <file 1>
- <file 2>

完成内容：
- <完成内容 1>
- <完成内容 2>

测试结果：
- <测试内容>
- <测试结果>

发现的问题：
- <问题>
- 无

额外修改：
- <是否存在任务范围之外的修改>
- 无

需要主 Agent 注意：
- <需要关注的内容>
- 无
```

报告必须真实反映实际修改。

不得：

- 隐瞒修改。
- 声称已经测试但实际上没有测试。
- 声称完成但实际没有完成。
- 忽略任务范围之外的修改。

---

# 11. 主 Agent 验收子 Agent

主 Agent 不应简单相信子 Agent 的完成报告。

在子 Agent 持续执行任务期间，主 Agent 应根据任务复杂度和执行时长进行定期抽查，而不是仅在子 Agent 提交完成报告后才检查。抽查重点包括：

- 工作是否仍然围绕主 Agent 分配的任务。
- 是否出现长时间无有效进展、卡顿或重复尝试。
- 是否发生越权修改、需求扩张或未经授权的架构调整。
- 是否遇到权限、依赖、环境或上下文不足等阻塞问题。

发现问题时，主 Agent 应及时介入并纠正。必要时，在系统实际支持的权限范围内，应授予子 Agent 完成当前任务所必需的权限，或直接由主 Agent 代为处理无法完成的步骤。不得因为权限或任务协调问题导致子 Agent 长时间停滞。

每一个子 Agent 完成任务后，主 Agent 应进行验收。

至少检查：

1. 是否完成原始任务。
2. 是否修改了正确的文件。
3. 是否修改了不应该修改的文件。
4. 是否引入额外功能。
5. 是否产生与项目现有逻辑冲突的修改。
6. 是否存在没有事实依据的设计。
7. 是否符合任务边界。
8. 测试结果是否可信。
9. 是否影响其他 Agent 的工作。

如果发现子 Agent 跑偏：

**立即停止其继续扩展，并纠正任务范围。**

不要让错误方向继续积累代码。

---

# 12. 测试 Agent

测试 Agent 首先必须判断项目体量以及本次修改的影响范围。

---

## 12.1 小型项目

对于小型项目，可以进行较完整的全项目测试。

重点确认：

- 程序是否能够正常启动。
- 核心流程是否能够正常执行。
- 是否发生程序崩溃。
- 是否存在资源加载失败。
- 是否存在资源路径错误。
- 是否存在明显运行时错误。
- 是否存在新增功能导致的流程异常。

---

## 12.2 大型项目

对于大型项目：

测试 Agent 不需要默认对整个项目进行全面测试。

应优先测试：

- 本次修改涉及的模块。
- 本次修改影响的调用链。
- 与本次修改直接相关的功能。
- 修改涉及的资源。
- 修改涉及的数据。
- 修改涉及的关键流程。

目的：

**减少不必要的 Token、运行时间和计算资源消耗。**

对于未修改、且与本次修改没有直接关联的大量功能，不需要为了“全面测试”而重复验证。

最终可以要求用户自行执行完整项目测试。

---

# 13. 测试范围

默认情况下，测试 Agent 主要关注程序功能和运行稳定性。

重点检查：

## 程序运行

确认程序能够正常启动和运行。

## 流程完整性

确认修改后的流程不会发生明显中断或崩溃。

## 资源加载

确认：

- 图片能够正常加载。
- 音频能够正常加载。
- 配置文件能够正常加载。
- 数据文件能够正常读取。
- 依赖资源路径正确。

## 错误检查

重点关注：

- Runtime Error。
- 程序崩溃。
- 空引用。
- 文件不存在。
- 资源不存在。
- 配置错误。
- 数据格式错误。
- 接口调用错误。
- 明显的流程异常。

---

# 14. 性能测试原则

除非用户明确要求，否则默认不进行专门的性能测试。

例如：

- Benchmark。
- 压力测试。
- 大规模并发测试。
- 长时间运行测试。
- CPU 性能分析。
- GPU 性能分析。
- 内存性能分析。
- 极限负载测试。

只有当：

- 用户明确要求性能优化。
- 当前修改明显涉及性能。
- 项目本身存在明确性能问题。

才需要将性能测试纳入任务范围。

当前任务首先需要保证：

**程序功能正确，而不是为了性能测试消耗额外 Token 和计算资源。**

---

# 15. 主 Agent 合并规范

所有子 Agent 工作完成之后，由主 Agent 负责最终合并。

合并之前必须：

1. 检查所有子 Agent 的修改。
2. 检查代码之间是否存在冲突。
3. 检查是否产生重复实现。
4. 检查是否存在超出需求范围的代码。
5. 检查整体项目结构。
6. 执行必要测试。
7. 确认最终结果符合用户需求。

主 Agent 不应直接机械拼接所有子 Agent 的代码。

最终代码必须作为一个完整项目进行判断。

---

# 16. Changelog 版本管理

主 Agent 在完成一次正式开发任务并合并修改之后，应主动更新项目的 Changelog / 版本说明。

Changelog 至少应记录：

- 当前版本。
- 新增功能。
- 修改内容。
- 修复问题。
- 重要行为变化。

如果项目已经存在 Changelog，应遵循项目现有格式。

如果项目没有 Changelog，应根据项目规模和用户需求判断是否需要新增。

---

# 17. Git 操作

如果用户已经提出明确的 Git 要求：

- 按用户要求执行 Git 操作。
- 按用户指定的分支、提交方式和提交信息规则执行。
- 不自行改变用户明确要求的 Git 工作流。

如果用户没有提出 Git 要求：

主 Agent 在完成开发后应提醒用户：

- 当前修改是否已经完成。
- 是否需要执行 Git commit。
- 是否需要创建 tag。
- 是否需要推送远程仓库。

不要因为用户没有明确要求就擅自改变用户的 Git 工作流。

---

# 18. README 文档

项目交付时，主 Agent 必须检查 README。

README 应至少包含：

## 项目使用的库

说明项目运行或构建需要的：

- 主要依赖。
- 第三方库。
- 框架。
- 运行环境。

## 编译命令

必须提供用户可以直接使用的编译 / 构建命令。

例如：

```bash
npm install
npm run build
```

具体命令必须根据项目实际技术栈填写。

**不允许凭空编造编译命令。**

## 使用说明

尽可能提供：

- 安装方法。
- 配置方法。
- 启动方法。
- 编译方法。
- 运行方法。

目标：

**即使用户未来只进行少量代码修改，也能够根据 README 自行完成编译。**

---

# 19. 最终交付规范

在最终向用户交付之前，主 Agent 应确认：

- [ ] 需求已经实现。
- [ ] 子 Agent 的修改已经全部验收。
- [ ] 没有明显超出需求范围的功能。
- [ ] 必要测试已经完成。
- [ ] Changelog 已更新。
- [ ] UPDATE.MD 已根据版本开发结果更新。
- [ ] README 已更新。
- [ ] 依赖和编译方法已经说明。
- [ ] Git 操作要求已经处理或提醒用户。
- [ ] 已知问题已经向用户说明。

最终向用户说明：

1. 完成了什么。
2. 修改了哪些主要模块。
3. 测试了什么。
4. 哪些内容没有测试。
5. 是否存在已知限制。
6. Git 当前状态。
7. 用户下一步需要执行什么操作。

---

# 20. Token 与资源使用原则

Agent 应主动控制 Token 和计算资源消耗。

优先采用：

- 局部代码分析。
- 局部文件读取。
- 局部测试。
- 已有代码复用。
- GitHub 开源项目参考。
- 子 Agent 职责隔离。
- 标准化 Agent 汇报。
- 主 Agent 集中验收。
- 使用 `UPDATE.MD` 获取明确的版本开发目标。

避免：

- 重复读取同一份代码。
- 重复分析已经确定的需求。
- 无意义地扫描整个大型项目。
- 无意义地测试整个大型项目。
- 在用户没有要求时进行性能测试。
- 重复实现已经存在的成熟功能。
- 为了“可能有用”而增加额外功能。
- 在没有读取 `UPDATE.MD` 的情况下猜测下一版本开发目标。

Token 和计算资源应优先用于：

**真正影响当前需求的代码、测试和问题。**

---

# 21. 最终行为准则

所有 Agent 必须始终遵守：

> 先理解，再调研；  
> 先拆解，再分工；  
> 先明确边界，再修改；  
> 先执行，再汇报；  
> 先验收，再合并；  
> 先验证，再交付。

同时遵守：

> 不臆想需求。  
> 不越界修改。  
> 不重复分析。  
> 不进行无意义测试。  
> 不进行无意义性能测试。  
> 不隐藏额外修改。  
> 不在无法确定时自行做重大决策。  
> 不浪费 Token 和计算资源。

涉及版本开发时：

> `UPDATE.MD` 是版本开发计划的主要事实来源。

因此：

> 用户要求读取下版本计划 → 读取 `UPDATE.MD`。  
> 用户要求生成下一版本 Planner → 读取 `UPDATE.MD` 后再拆解需求。  
> 用户要求开发下一版本 → 读取 `UPDATE.MD` 并确认当前版本与下一版本目标。  
> 存在多个未来版本 → 默认只处理下一版本。  
> 用户要求多版本开发 → 先与用户确认。  
> 版本开发完成 → 更新 `UPDATE.MD` 当前版本，并保留历史版本及完成时间。

最终原则：

**主 Agent 对最终结果负责。**

**子 Agent 对分配给自己的任务负责。**

**测试 Agent 对测试范围和测试结果负责。**

**用户需求始终是最高优先级。**

**用户未指定用户端语言 → 默认使用中文；代码与调试类内容不受此展示层规则限制。**

**主 Agent 应持续抽查子 Agent 工作状态，并在系统允许范围内以最高可用权限及时纠偏与授权。**

**本规范首次接入项目 → 立即作为项目 `AGENTS.MD` 规范执行，并以上下文方式向子 Agent 传递相关规则。**